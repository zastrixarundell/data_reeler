services:
  database:
    image: postgres:alpine
    name: postgres
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - 5432:5432
  rabbitmq:
    image: rabbitmq:management-alpine
    name: rabbitmq
    hostname: data_reeler_rabbitmq
    ports:
      - 5672:5672
      - 8080:15672